import { PreferencesLocalStorageWrapper } from './preferences-local-storage-wrapper';

describe('preferencesLocalStorageWrapper', () => {
  it('should work', () => {
    expect(PreferencesLocalStorageWrapper).toEqual('project-one-client-models');
  });
});
