import { sortSearchPage } from './sort-search-page';

describe('projectOneSharedSortSearchPage', () => {
  it('should work', () => {
    expect(sortSearchPage()).toEqual(
      'project-one-shared-sort-search-page'
    );
  });
});
